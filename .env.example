NEXT_PUBLIC_SITE_URL =

# --- Contact form email (SMTP via Nodemailer) -------------------
# For Gmail: enable 2-Step Verification on the account, then create an
# App Password at https://myaccount.google.com/apppasswords and use it
# as SMTP_PASSWORD below (not your normal Gmail password).
SMTP_HOST = smtp.gmail.com
SMTP_PORT = 587
SMTP_SECURE = false
SMTP_USER =
SMTP_PASSWORD =

GITHUB_TOKEN =
CONVERTKIT_FORM_ID =
CONVERTKIT_API_KEY =
CONVERTKIT_API_URL = https://api.convertkit.com/v3/
ESLINT_NO_DEV_ERRORS = true
ANALYZE = false
